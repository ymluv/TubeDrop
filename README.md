# TubeDrop

Программа для скачивания видео и аудио с YouTube через `yt-dlp`. 
- Используются только ресурсы вашего ПК.
- Никакой рекламы перед скачиванием.
- Никаких платных подписок чтобы скачать видео в 4K.
- Никаких файлов с названием `SLAVA_UKRAINI` создавать не нужно чтобы получить доступ к ПО на териториии РБ и РФ.

## Запуск

### Готовый exe

1. Скачайте `TubeDrop-windows-x64.zip` из Releases.
2. Распакуйте архив.
3. Откройте `TubeDrop.exe`.

Python и зависимости устанавливать не нужно.

### Из исходников

1. Откройте `run.bat`.
2. Дождитесь запуска приложения.

При первом запуске создается `.venv` и устанавливаются зависимости из `requirements.txt`.
После запуска приложения окно `cmd` закрывается.

Для запуска из исходников нужен Python 3.10+.

## Использование

1. Вставьте ссылку на YouTube-видео.
2. Нажмите `Получить`.
3. Выберите качество.
4. Выберите папку.
5. При необходимости введите имя файла.
6. Нажмите `Скачать`.

Если поле имени файла пустое, используется автоназвание:

```text
название видео [id]
```

Кнопка `Отмена` останавливает текущее скачивание.

После завершения доступны кнопки:

- `OK`;
- `Открыть файл`;
- `Открыть папку`.

## Форматы

- Видео сохраняется в MP4.
- Для видео используется AAC-аудио: 192 kbps, 48 kHz, stereo.
- Доступные аудиоформаты: M4A, MP3, WAV.
- MP3 сохраняется как 192 kbps CBR, 48 kHz, stereo, ID3v2.3.
- Превью видео сохраняется в ту же папку.

Опция `Строго MP4` может увеличить время подготовки файла.

## Вход в YouTube

Если YouTube требует вход или проверку:

1. Нажмите `Проверка`.
2. Нажмите `Открыть вход`.
3. Войдите в Google/YouTube в открывшемся браузере.
4. Вернитесь в приложение.
5. Нажмите `Готово`.

Используется отдельный профиль браузера TubeDrop.

Папка профиля в exe-сборке:

```text
%LOCALAPPDATA%\TubeDrop\.tubedrop_secure_browser
```

Кнопка `Сброс` удаляет сохраненную сессию.

## Сборка exe

```powershell
powershell -ExecutionPolicy Bypass -File .\build_exe.ps1
```

Результат:

```text
dist\TubeDrop.exe
dist\TubeDrop-windows-x64.zip
```

В exe включены Python, PySide6, `yt-dlp`, Node.js и ffmpeg.

## Совместимость

Поддерживается:

- Windows 10 x64;
- Windows 11 x64.

Не является готовой версией для:

- Windows 7/8/8.1;
- Linux;
- macOS.

## Зависимости

- `yt-dlp`;
- `yt-dlp-ejs`;
- `nodejs-wheel-binaries`;
- `PySide6`;
- `Pillow`;
- `requests`;
- `imageio-ffmpeg`.
